export interface CityDTO {
  countryiso2: string;
  stateCode: string;
  state: string;
  provinceCode: string;
  province: string;
  departmentCode: string;
  department: string;
  code: string;
  label: string;
}
